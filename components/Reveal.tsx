/**
 * The entrance animation is now pure CSS (`.reveal` in globals.css), which can
 * never leave content stuck hidden. This component is intentionally a no-op,
 * kept so the layout import stays valid.
 */
export function Reveal() {
  return null;
}
